import { View, Text } from 'react-native';

export default function App() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ fontSize: 24, marginBottom: 20 }}>Welcome to Chatt</Text>
      <Text style={{ fontSize: 16, color: '#666' }}>Loading demo version...</Text>
    </View>
  );
}